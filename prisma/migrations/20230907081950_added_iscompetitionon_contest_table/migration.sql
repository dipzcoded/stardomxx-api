BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[contests] ADD [isCompetionOn] BIT NOT NULL CONSTRAINT [contests_isCompetionOn_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
