import accountActivationValidator from "./account-activation.validator";
import forgotPasswordValidator from "./forgot-password.validator";
import loginValidator from "./login.validator";
import registrationValidator from "./registration.validator";
import resetPasswordValidator from "./reset-password.validator";

export {
  accountActivationValidator,
  forgotPasswordValidator,
  loginValidator,
  registrationValidator,
  resetPasswordValidator,
};
